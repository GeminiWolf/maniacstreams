import {createTheme} from '@mui/material/styles'

export const TextThemes = createTheme({
  palette: {
    primary: {
      // Purple and green play nicely together.
      main: '#fff',
    },
    secondary: {
      // This is green.A700 as hex.
      main: '#111',
    },
  },
});
